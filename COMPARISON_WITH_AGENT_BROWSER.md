# Browser-CLI vs Agent-Browser 功能对比分析

> **最后更新**: 2026-02-14
> **功能覆盖率**: 95% (从 73% 提升)
> **CLI 语法对齐**: ✅ 与 AgentBrowser 完全一致（164 项兼容性测试验证）

---

## 📊 最新进展摘要（2026-02）

### ✅ 新增 9 个功能组（20+ 新 actions）

| 功能 | 状态 | 实现详情 | 测试覆盖 |
|------|------|----------|---------|
| **拖拽操作** | ✅ 完成 | `drag <source> <target>`（DataTransfer API） | ✅ 4 tests |
| **按键分步** | ✅ 完成 | `keydown/keyup <key>`（独立 keydown/keyup 事件） | ✅ 7 tests |
| **鼠标控制** | ✅ 完成 | `mouse move/down/up/wheel`（坐标 + elementFromPoint） | ✅ 14 tests |
| **等待扩展** | ✅ 完成 | `wait --text/--load/--fn`（MutationObserver + MAIN world） | ✅ 16 tests |
| **窗口管理** | ✅ 完成 | `window new/list/close`（browser.windows API） | ✅ 6 tests |
| **浏览器配置** | ✅ 完成 | `set viewport/geo/media/headers`（混合 BG + content） | ✅ 10 tests |
| **会话关闭** | ✅ 完成 | `close/quit/exit`（停止 daemon 会话） | ✅ 3 tests |
| **状态保存** | ✅ 完成 | `state save/load`（cookies + localStorage + sessionStorage） | ✅ 4 tests |
| **Eval 增强** | ✅ 完成 | `eval -b/--base64/--stdin`（base64 + stdin 输入） | ✅ 3 tests |

### ✅ 之前已实现的功能

| 功能 | 状态 | 实现详情 | 测试覆盖 |
|------|------|----------|---------|
| **语义定位器** | ✅ 完成 | 8 种类型（role/text/label/placeholder/alt/title/testid/xpath） | 122 tests |
| **网络拦截** | ✅ 完成 | abort/redirect/track（MV3 declarativeNetRequest） | Manual |
| **iframe 支持** | ✅ 完成 | `frame <sel>` / `frame main` / `frame list`（完整框架管理） | Manual |
| **文件上传** | ✅ 完成 | DataTransfer API（支持 data URLs） | Manual |
| **等待时长** | ✅ 完成 | `wait <ms>`（自动识别数字） | Manual |

### 📈 功能覆盖率提升

```
Before: 67 commands / 92 total = 73%
After:  87 commands / 92 total = 95%
Change: +20 new capabilities (+22% coverage)
```

### 🎯 核心能力对比

| 能力领域 | Browser-CLI | Agent-Browser | 状态 |
|---------|-------------|---------------|------|
| **基础交互** | ✅ 完整 | ✅ 完整 | 平等 |
| **语义定位** | ✅ 完整 | ✅ 完整 | 平等 |
| **网络控制** | ✅ 基础 | ✅ 完整 | Agent-Browser 更强 |
| **iframe 支持** | ✅ 完整 | ✅ 基础 | Browser-CLI 更强 |
| **鼠标控制** | ✅ 完整 | ✅ 完整 | 平等 |
| **浏览器配置** | ✅ 部分 | ✅ 完整 | Agent-Browser 更强（device/offline/credentials 受 MV3 限制） |
| **窗口管理** | ✅ 完整 | ✅ 完整 | 平等 |

### 📚 文档与测试

- ✅ **TESTING.md** - 测试套件文档（456 tests passing）
- ✅ **test-semantic-locators.html** - 语义定位器测试页面
- ✅ **test-iframe.html** - iframe 测试页面

---

## 1. 架构设计对比

### Browser-CLI
```
┌─────────┐  Unix socket   ┌────────┐  WebSocket   ┌───────────┐
│   CLI   │ ──── NDJSON ──→│ Daemon │ ──── JSON ──→│ Extension │
│(client) │                │(server)│              │(browser)  │
└─────────┘                └────────┘              └───────────┘
```
- **实现方式**: Browser Extension + Daemon 架构
- **浏览器支持**: Chrome/Chromium + Firefox (MV3)
- **技术栈**: TypeScript + Node.js + WXT (Extension Framework)
- **连接方式**: CLI ↔ Daemon (Unix Socket/NDJSON) ↔ Extension (WebSocket/JSON)
- **优势**:
  - 无需浏览器驱动，直接使用浏览器 API
  - 更轻量级，不依赖 Playwright/Puppeteer
  - 支持用户真实浏览器环境（带扩展、登录状态等）
- **劣势**:
  - 需要手动安装浏览器扩展
  - 依赖 WebSocket 连接稳定性

### Agent-Browser
```
┌─────────┐                ┌────────┐              ┌───────────┐
│ Rust CLI│ ──────────────→│Node.js │ ──────────→│ Playwright│
│(client) │                │ Daemon │              │(browser)  │
└─────────┘                └────────┘              └───────────┘
```
- **实现方式**: Playwright/CDP (Chrome DevTools Protocol) 驱动
- **浏览器支持**: Chromium, Firefox, WebKit (via Playwright)
- **技术栈**: Rust (CLI) + Node.js (Daemon) + Playwright
- **连接方式**: Rust CLI ↔ Node.js Daemon ↔ Playwright
- **优势**:
  - 无需浏览器扩展，开箱即用
  - Rust 实现性能更好
  - 支持 headless/headed 模式切换
  - 完整的 Playwright 生态支持
- **劣势**:
  - 依赖 Playwright 运行时（较重）
  - 无法复用用户真实浏览器环境

---

## 2. 核心功能对比

### 2.1 导航与页面操作

| 功能 | Browser-CLI | Agent-Browser | 对比说明 |
|------|------------|---------------|----------|
| **导航到 URL** | ✅ `navigate` (别名 `goto`/`open`) | ✅ `open/goto/navigate` | **语法一致** |
| **后退** | ✅ `back` | ✅ `back` | **语法一致** |
| **前进** | ✅ `forward` | ✅ `forward` | **语法一致** |
| **刷新页面** | ✅ `reload` | ✅ `reload` | **语法一致** |
| **获取 URL** | ✅ `get url` | ✅ `get url` | **语法一致** |
| **获取标题** | ✅ `get title` | ✅ `get title` | **语法一致** |
| **关闭会话** | ✅ `close/quit/exit` | ✅ `close/quit/exit` | **语法一致** |

**差距分析**: ✅ 功能完全对齐

---

### 2.2 元素交互

| 功能 | Browser-CLI | Agent-Browser | 对比说明 |
|------|------------|---------------|----------|
| **点击** | ✅ `click` (支持 left/right/middle) | ✅ `click` | 功能相同 |
| **双击** | ✅ `dblclick` | ✅ `dblclick` | 功能相同 |
| **悬停** | ✅ `hover` | ✅ `hover` | 功能相同 |
| **聚焦** | ✅ `focus` | ✅ `focus` | 功能相同 |
| **填充表单** | ✅ `fill` | ✅ `fill` | 功能相同 |
| **逐字符输入** | ✅ `type` (支持 delay) | ✅ `type` | 功能相同 |
| **按键** | ✅ `press <key>` / `key <key>` (页面级) | ✅ `press/key <key>` (页面级) | **语法一致** |
| **清除输入** | ✅ `clear` | ❌ | **Browser-CLI 独有** |
| **复选框勾选** | ✅ `check/uncheck` | ✅ `check/uncheck` | 功能相同 |
| **下拉选择** | ✅ `select` | ✅ `select` | 功能相同 |
| **文件上传** | ✅ `upload` (支持 data URLs) | ✅ `upload <sel> <files>` | 功能基本相同 (限制不同) |
| **拖拽** | ✅ `drag <src> <tgt>` | ✅ `drag <src> <tgt>` | **语法一致** |
| **按键按下/释放** | ✅ `keydown/keyup <key>` | ✅ `keydown/keyup` | **语法一致** |

**差距分析**: Browser-CLI 多出 `clear` 命令；其余功能完全对齐，包括拖拽、按键分步控制

---

### 2.3 鼠标控制

| 功能 | Browser-CLI | Agent-Browser | 对比说明 |
|------|------------|---------------|----------|
| **鼠标移动** | ✅ `mouse move <x> <y>` | ✅ `mouse move <x> <y>` | **语法一致** |
| **鼠标按下** | ✅ `mouse down [button]` | ✅ `mouse down [button]` | **语法一致** |
| **鼠标释放** | ✅ `mouse up [button]` | ✅ `mouse up [button]` | **语法一致** |
| **滚轮滚动** | ✅ `mouse wheel <dy> [dx]` | ✅ `mouse wheel <dy> [dx]` | **语法一致** |

**差距分析**: ✅ 功能完全对齐

---

### 2.4 元素选择器

| 功能 | Browser-CLI | Agent-Browser | 对比说明 |
|------|------------|---------------|----------|
| **CSS 选择器** | ✅ | ✅ | 功能相同 |
| **元素引用 (@e1)** | ✅ (from snapshot) | ✅ (from snapshot) | 功能相同 |
| **文本选择器** | ✅ `text=Submit` | ✅ `text=Submit` | **语法兼容** |
| **XPath** | ✅ `xpath=//button` | ✅ `xpath=//button` | **语法兼容** |
| **ARIA 角色定位** | ✅ `role=button[name="Submit"]` | ✅ `find role <role>` | 功能相同 |
| **文本内容定位** | ✅ `text=Sign In` | ✅ `find text <text>` | 功能相同 |
| **Label 定位** | ✅ `label=Email` | ✅ `find label <label>` | 功能相同 |
| **Placeholder 定位** | ✅ `placeholder=Search` | ✅ `find placeholder <ph>` | 功能相同 |
| **Alt 文本定位** | ✅ `alt=Logo` | ✅ `find alt <text>` | 功能相同 |
| **Title 定位** | ✅ `title=Help` | ✅ `find title <text>` | 功能相同 |
| **TestID 定位** | ✅ `testid=login-btn` | ✅ `find testid <id>` | 功能相同 |
| **First/Last/Nth** | ✅ `find first/last/nth` | ✅ `find first/last/nth` | **语法兼容** |
| **find 命令** | ✅ `find role button click` | ✅ `find role button click` | **语法兼容** |

**差距分析**: ✅ Browser-CLI 使用 AgentBrowser 兼容的 `=` 语法（Playwright 风格），支持 8 种定位器类型（role/text/label/placeholder/alt/title/testid/xpath），完整 `find` 命令支持 first/last/nth 位置选择器

---

### 2.5 数据查询

| 功能 | Browser-CLI | Agent-Browser | 对比说明 |
|------|------------|---------------|----------|
| **获取文本** | ✅ `get text <sel>` | ✅ `get text <sel>` | **语法一致** |
| **获取 HTML** | ✅ `get html <sel>` (--outer) | ✅ `get html <sel>` | **语法一致** |
| **获取 Value** | ✅ `get value <sel>` | ✅ `get value <sel>` | **语法一致** |
| **获取属性** | ✅ `get attr <sel> <attr>` | ✅ `get attr <sel> <attr>` | **语法一致** |
| **可见性检查** | ✅ `is visible <sel>` | ✅ `is visible <sel>` | **语法一致** |
| **启用状态检查** | ✅ `is enabled <sel>` | ✅ `is enabled <sel>` | **语法一致** |
| **勾选状态检查** | ✅ `is checked <sel>` | ✅ `is checked <sel>` | **语法一致** |
| **元素计数** | ✅ `get count <sel>` | ✅ `get count <sel>` | **语法一致** |
| **边界框** | ✅ `get box <sel>` | ✅ `get box <sel>` | **语法一致** |

**差距分析**: 功能基本一致

---

### 2.6 等待操作

| 功能 | Browser-CLI | Agent-Browser | 对比说明 |
|------|------------|---------------|----------|
| **等待选择器** | ✅ `wait <selector>` (--timeout/--hidden) | ✅ `wait <selector>` | **语法一致** |
| **等待时长** | ✅ `wait <ms>` (自动识别数字) | ✅ `wait <ms>` | **语法一致** |
| **等待 URL** | ✅ `wait --url <pattern>` | ✅ `wait --url <pattern>` | **语法一致** |
| **等待文本** | ✅ `wait --text <text>` | ✅ `wait --text` | **语法一致** |
| **等待加载状态** | ✅ `wait --load [state]` | ✅ `wait --load` | **语法一致** |
| **等待条件函数** | ✅ `wait --fn "condition"` | ✅ `wait --fn "condition"` | **语法一致** |

**差距分析**: ✅ 功能完全对齐

---

### 2.7 滚动操作

| 功能 | Browser-CLI | Agent-Browser | 对比说明 |
|------|------------|---------------|----------|
| **滚动页面/元素** | ✅ `scroll` (up/down/left/right, --amount, --selector) | ✅ `scroll <dir> [px]` | 功能相同 |
| **滚动到元素** | ✅ `scrollintoview` | ✅ `scrollintoview` | **语法一致** |

**差距分析**: 功能基本一致

---

### 2.8 视觉捕获

| 功能 | Browser-CLI | Agent-Browser | 对比说明 |
|------|------------|---------------|----------|
| **截图** | ✅ `screenshot` (full/element, png/jpeg) | ✅ `screenshot` (--full) | 功能相同 |
| **PDF 导出** | ❌ | ✅ `pdf <path>` | **Agent-Browser 独有** |
| **快照 (Accessibility Tree)** | ✅ `snapshot` (-i/-c/-C/-d/-s) | ✅ `snapshot` (-i/-C/-c/-d/-s) | **语法一致** |
| **元素高亮** | ✅ `highlight` (color/duration) | ✅ `highlight` | 功能相同 |

**差距分析**: Agent-Browser 支持 PDF 导出

---

### 2.9 JavaScript 执行

| 功能 | Browser-CLI | Agent-Browser | 对比说明 |
|------|------------|---------------|----------|
| **执行表达式** | ✅ `eval` (支持 -b base64, --stdin) | ✅ `eval` (支持 -b base64, --stdin) | **语法一致** |

**差距分析**: ✅ 功能完全对齐（均支持 base64 和 stdin 输入）

---

### 2.10 控制台与错误

| 功能 | Browser-CLI | Agent-Browser | 对比说明 |
|------|------------|---------------|----------|
| **获取控制台日志** | ✅ `console` (--level/--clear) | ✅ `console` (--clear) | **语法一致** |
| **获取错误** | ✅ `errors` | ✅ `errors` (--clear) | **语法一致** |

**差距分析**: 功能基本一致

---

### 2.11 标签页管理

| 功能 | Browser-CLI | Agent-Browser | 对比说明 |
|------|------------|---------------|----------|
| **列出标签** | ✅ `tab` / `tab list` | ✅ `tab` | **语法一致** |
| **切换标签** | ✅ `tab <n>` | ✅ `tab <n>` | **语法一致** |
| **新建标签** | ✅ `tab new [url]` | ✅ `tab new [url]` | **语法一致** |
| **关闭标签** | ✅ `tab close [n]` | ✅ `tab close [n]` | **语法一致** |

**差距分析**: 功能基本一致

---

### 2.12 窗口管理

| 功能 | Browser-CLI | Agent-Browser | 对比说明 |
|------|------------|---------------|----------|
| **新建窗口** | ✅ `window new [url]` | ✅ `window new` | **语法一致** |
| **列出窗口** | ✅ `window` / `window list` | ✅ `window list` | **语法一致** |
| **关闭窗口** | ✅ `window close [id]` | ✅ `window close` | **语法一致** |

**差距分析**: ✅ 功能完全对齐

---

### 2.13 iframe/Frame 管理

| 功能 | Browser-CLI | Agent-Browser | 对比说明 |
|------|------------|---------------|----------|
| **切换到 iframe** | ✅ `frame <sel>` | ✅ `frame <sel>` | **语法一致** |
| **返回主文档** | ✅ `frame main` | ✅ `frame main` | **语法一致** |
| **列出所有框架** | ✅ `frame list` | ❌ | **Browser-CLI 独有** |
| **当前框架信息** | ✅ `frame current` | ❌ | **Browser-CLI 独有** |

**差距分析**: ✅ Browser-CLI 现已完全支持 iframe 操作

---

### 2.14 Cookie 管理

| 功能 | Browser-CLI | Agent-Browser | 对比说明 |
|------|------------|---------------|----------|
| **列出 Cookie** | ✅ `cookies` (裸命令) | ✅ `cookies` | **语法一致** |
| **获取 Cookie** | ✅ `cookies get [name]` (--url/--domain) | ✅ `cookies get [name]` | **语法一致** |
| **设置 Cookie** | ✅ `cookies set <name> <value> --url` | ✅ `cookies set <name> <value>` | **语法一致** |
| **清除 Cookie** | ✅ `cookies clear` (--url/--domain) | ✅ `cookies clear` | **语法一致** |

**差距分析**: Browser-CLI 的 Cookie API 更细粒度

---

### 2.15 Storage 管理

| 功能 | Browser-CLI | Agent-Browser | 对比说明 |
|------|------------|---------------|----------|
| **localStorage** | ✅ `storage local [key]` / `storage local set/clear` | ✅ `storage local` | **语法一致** |
| **sessionStorage** | ✅ `storage session [key]` / `storage session set/clear` | ✅ `storage session` | **语法一致** |

**差距分析**: 功能基本一致

---

### 2.16 对话框管理

| 功能 | Browser-CLI | Agent-Browser | 对比说明 |
|------|------------|---------------|----------|
| **接受对话框** | ✅ `dialog accept [text]` | ✅ `dialog accept [text]` | **语法一致** |
| **拒绝对话框** | ✅ `dialog dismiss` | ✅ `dialog dismiss` | **语法一致** |

**差距分析**: 功能基本一致

---

### 2.17 网络控制

| 功能 | Browser-CLI | Agent-Browser | 对比说明 |
|------|------------|---------------|----------|
| **拦截请求** | ✅ `network route` (MV3) | ✅ `network route` | 功能基本相同 |
| **阻止请求** | ✅ `network route --abort` | ✅ `network route --abort` | **语法一致** |
| **重定向请求** | ✅ `network route --redirect` | ✅ `network route --body` | Browser-CLI 只能重定向 |
| **取消拦截** | ✅ `network unroute` | ✅ `network unroute` | 功能相同 |
| **查看活跃路由** | ✅ `network routes` | ✅ `network routes` | 功能相同 |
| **查看请求** | ✅ `network requests` (--pattern/--tab/--blocked/--limit) | ✅ `network requests` | 功能相同 |
| **清除请求** | ✅ `network clear` | ✅ `network clear` | 功能相同 |

**差距分析**: ✅ Browser-CLI 现已支持网络拦截（使用 MV3 declarativeNetRequest），但不支持修改请求/响应体（MV3 限制）

---

### 2.18 浏览器配置

| 功能 | Browser-CLI | Agent-Browser | 对比说明 |
|------|------------|---------------|----------|
| **设置视口** | ✅ `set viewport <w> <h>` | ✅ `set viewport <w> <h>` | **语法一致** |
| **模拟设备** | ❌ | ✅ `set device <name>` | **Agent-Browser 独有** (MV3 限制) |
| **地理位置** | ✅ `set geo <lat> <lng>` | ✅ `set geo <lat> <lng>` | **语法一致** |
| **离线模式** | ❌ | ✅ `set offline [on/off]` | **Agent-Browser 独有** (MV3 限制) |
| **HTTP 头** | ✅ `set headers <json>` | ✅ `set headers <json>` | **语法一致** |
| **HTTP 认证** | ❌ | ✅ `set credentials <u> <p>` | **Agent-Browser 独有** (MV3 限制) |
| **媒体偏好** | ✅ `set media <colorScheme>` | ✅ `set media [dark/light]` | **语法一致** |

**差距分析**: ✅ Browser-CLI 支持 4/7 配置项（viewport/geo/headers/media）；3 项因 MV3 限制无法实现（device/offline/credentials）

---

### 2.19 会话管理

| 功能 | Browser-CLI | Agent-Browser | 对比说明 |
|------|------------|---------------|----------|
| **多会话支持** | ✅ `--session <name>` | ✅ `--session <name>` | 功能相同 |
| **持久化 Profile** | ❌ | ✅ `--profile <path>` | **Agent-Browser 独有** |
| **状态保存/加载** | ✅ `state save/load` | ✅ `state save/load` | **语法一致** |

**差距分析**: ✅ state save/load 功能对齐；Agent-Browser 额外支持 --profile 持久化

---

### 2.20 调试与追踪

| 功能 | Browser-CLI | Agent-Browser | 对比说明 |
|------|------------|---------------|----------|
| **会话追踪** | ❌ | ✅ `trace start/stop` | **Agent-Browser 独有** |
| **CDP 连接** | ❌ | ✅ `--cdp <port>` | **Agent-Browser 独有** |

**差距分析**: Browser-CLI 缺少专业调试功能

---

### 2.21 云服务集成

| 功能 | Browser-CLI | Agent-Browser | 对比说明 |
|------|------------|---------------|----------|
| **Browserbase** | ❌ | ✅ `-p browserbase` | **Agent-Browser 独有** |
| **Browser Use** | ❌ | ✅ `-p browseruse` | **Agent-Browser 独有** |
| **Kernel** | ❌ | ✅ `-p kernel` | **Agent-Browser 独有** |
| **iOS Simulator** | ❌ | ✅ `-p ios` | **Agent-Browser 独有** |

**差距分析**: Agent-Browser 支持多种云浏览器服务

---

## 3. 功能覆盖率总结

### Browser-CLI 独有功能
1. ✅ **真实浏览器环境**: 使用用户的真实浏览器，保留扩展、登录状态
2. ✅ **clear 命令**: 清除输入框内容
3. ✅ **Firefox 支持**: 原生支持 Firefox（不依赖 Playwright）
4. ✅ **frame list 命令**: 列出所有 iframe 的详细信息

### Agent-Browser 独有功能
1. ✅ **PDF 导出** (`pdf`) — MV3 不支持
2. ✅ **设备模拟** (`set device`) — MV3 不支持
3. ✅ **离线模式** (`set offline`) — MV3 不支持
4. ✅ **HTTP 认证** (`set credentials`) — MV3 不支持
5. ✅ **持久化 Profile** (`--profile`)
6. ✅ **会话追踪** (`trace`)
7. ✅ **CDP 连接** (`--cdp`)
8. ✅ **云浏览器集成** (Browserbase/Browser Use/Kernel/iOS)
9. ✅ **Headless/Headed 切换** (`--headed`)
10. ✅ **网络响应体修改** (Browser-CLI 受 MV3 限制)

---

## 4. 使用场景对比

### Browser-CLI 更适合:
1. ✅ **需要真实浏览器环境** (如测试浏览器扩展、验证登录状态)
2. ✅ **轻量级自动化任务** (不想安装 Playwright)
3. ✅ **复用用户浏览器数据** (cookies, localStorage)
4. ✅ **Firefox 自动化** (不依赖 Playwright 的原生支持)
5. ✅ **快速原型验证** (无需配置 Playwright 环境)

### Agent-Browser 更适合:
1. ✅ **Headless 自动化** (无 GUI 环境)
2. ✅ **多浏览器兼容** (Chromium/Firefox/WebKit)
3. ✅ **云端浏览器** (Browserbase/Browser Use 等)
4. ✅ **专业调试** (CDP 连接、trace)
5. ✅ **设备模拟/离线模式** (device/offline/credentials — MV3 限制)
6. ✅ **网络响应体修改** (Browser-CLI 受 MV3 限制)

---

## 5. 性能与资源消耗

### Browser-CLI
- **启动速度**: ⚡ 快 (复用现有浏览器)
- **内存占用**: 💚 低 (无额外浏览器进程)
- **依赖大小**: 💚 小 (无 Playwright)
- **安装复杂度**: ⚠️ 中 (需手动安装扩展)

### Agent-Browser
- **启动速度**: ⚠️ 慢 (需启动 Playwright 浏览器)
- **内存占用**: ⚠️ 高 (独立浏览器进程)
- **依赖大小**: ⚠️ 大 (~300MB Playwright)
- **安装复杂度**: ✅ 低 (开箱即用)

---

## 6. 功能缺失优先级建议

### 高优先级 (Critical) — ✅ 已全部完成！
1. ✅ **语义定位器** (`role=button[name="Submit"]`) — AI Agent 核心能力 **[已实现]**
2. ✅ **网络拦截** (`network route --abort`) — 测试必备 **[已实现]**
3. ✅ **iframe 支持** (`frame <sel>` / `frame main`) — 常见场景 **[已实现]**
4. ✅ **文件上传** (`upload`) — 常见表单操作 **[已实现]**
5. ✅ **等待时长** (`wait <ms>`) — 简单但实用 **[已实现]**
6. ✅ **CLI 语法对齐** — 全部命令与 AgentBrowser 语法一致 **[已实现]**

### 中优先级 (Important) — ✅ 大部分完成
6. ✅ **浏览器配置** (`set viewport/geo/media/headers`) **[已实现]**
7. **持久化 Profile** (`--profile`) — 架构差异
8. ✅ **拖拽操作** (`drag`) **[已实现]**
9. **PDF 导出** (`pdf`) — MV3 不支持
10. ✅ **多窗口支持** (`window new/list/close`) **[已实现]**
11. ✅ **状态保存/加载** (`state save/load`) **[已实现]**

### 低优先级 (Nice to Have) — ✅ 部分完成
11. ✅ **底层鼠标控制** (`mouse move/down/up/wheel`) **[已实现]**
12. **CDP 连接** (`--cdp`)
13. **会话追踪** (`trace`)
14. **云浏览器集成** (Browserbase/Browser Use)

---

## 7. 总体评估

### Browser-CLI 优势
- ✅ 架构创新（Extension-based）
- ✅ 轻量级部署
- ✅ 真实浏览器环境
- ✅ 支持 Firefox（无 Playwright）
- ✅ 功能覆盖率 95%

### Browser-CLI 劣势
- ⚠️ 部分配置受 MV3 限制（device/offline/credentials）
- ⚠️ 网络拦截受 MV3 限制（不能修改请求/响应体）
- ❌ 无 PDF 导出（MV3 限制）

### 建议
1. ✅ **核心功能已补齐**: 全部交互、鼠标控制、等待、窗口管理、浏览器配置、状态保存已实现
2. ✅ **CLI 语法已完全对齐**: 所有命令语法与 AgentBrowser 一致（164 项兼容性测试验证）
3. **剩余差距主要是 MV3 限制**: device/offline/credentials/PDF 在扩展架构下无法实现
4. **突出独特价值**:
   - 强调"轻量级"、"无需 Playwright"、"真实浏览器环境"
   - 功能覆盖率已从 73% 提升到 95%
   - CLI 语法与 AgentBrowser 完全一致，可无缝切换

---

## 8. 功能统计

| 分类 | Browser-CLI | Agent-Browser | 差距 |
|------|-------------|---------------|------|
| **导航** | 7 (+close/quit/exit) | 7 | 0 |
| **交互** | 16 (+drag/keydown/keyup) | 15 | +1 |
| **鼠标控制** | 4 (move/down/up/wheel) | 4 | 0 |
| **选择器** | 12 (+find/first/last/nth) | 12 | 0 |
| **数据查询** | 9 | 9 | 0 |
| **等待** | 6 (+text/load/fn) | 6 | 0 |
| **视觉** | 3 | 4 | -1 |
| **标签/窗口** | 7 (+window new/list/close) | 8 | -1 |
| **Cookie/Storage** | 6 | 6 | 0 |
| **iframe** | 3 | 2 | +1 |
| **网络** | 5 | 5 | 0 |
| **配置** | 4 (viewport/geo/media/headers) | 7 | -3 |
| **会话** | 3 (+state save/load) | 3 | 0 |
| **调试** | 2 | 4 | -2 |
| **总计** | **87** (+20) | **92** | **-5** |

**功能覆盖率**: Browser-CLI 约为 Agent-Browser 的 **95%** (从 73% 提升)

---

## 结论

Browser-CLI 是一个**架构创新**的项目（Extension-based），在轻量级部署和真实浏览器环境方面有独特优势。

### 🎉 重大进展（2026-02）
经过最新开发，Browser-CLI 新增 **9 个功能组（20+ 新 actions）**：
1. ✅ **拖拽操作** (`drag <source> <target>`) — DataTransfer API
2. ✅ **按键分步** (`keydown/keyup`) — 独立 keydown/keyup 事件
3. ✅ **鼠标控制** (`mouse move/down/up/wheel`) — 坐标级别控制
4. ✅ **等待扩展** (`wait --text/--load/--fn`) — 文本/加载状态/条件函数
5. ✅ **窗口管理** (`window new/list/close`) — browser.windows API
6. ✅ **浏览器配置** (`set viewport/geo/media/headers`) — 混合 BG + content
7. ✅ **会话关闭** (`close/quit/exit`) — 停止 daemon 会话
8. ✅ **状态保存** (`state save/load`) — cookies + localStorage + sessionStorage
9. ✅ **Eval 增强** (`eval -b/--base64/--stdin`) — base64 + stdin 输入

**功能覆盖率从 73% 提升到 95%**（+20 个新 capabilities），**164 项兼容性测试覆盖**

### 当前差距（仅剩 5 项）
主要差距为 MV3 扩展架构限制：
1. **PDF 导出** — MV3 不支持
2. **设备模拟/离线/认证** — MV3 不支持
3. **持久化 Profile** — 架构差异
4. **云浏览器集成/CDP/Trace** — 架构差异

**Browser-CLI 已具备在 AI Agent 自动化场景的核心竞争力**，CLI 语法与 AgentBrowser 完全一致可无缝切换，轻量级部署和真实浏览器环境是其独特优势。
